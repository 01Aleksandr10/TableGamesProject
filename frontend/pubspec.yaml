name: tfp_flutter
description: TFP — Поиск игроков в настольные игры (по ТЗ)

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.4.3
  shared_preferences: ^2.3.0
  jwt_decoder: ^2.0.0
  web_socket_channel: ^3.0.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true